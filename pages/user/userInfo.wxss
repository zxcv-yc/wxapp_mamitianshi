/* switch 颜色设置 */

switch .wx-switch-input:not([class*="bg-"]) {
    background: var(--pink) !important;
}

.cu-form-group picker {
    flex: 1;
    padding-right: 40rpx;
    overflow: hidden;
    position: relative;
}

.biaoqian {
    border-radius: 6rpx;
}